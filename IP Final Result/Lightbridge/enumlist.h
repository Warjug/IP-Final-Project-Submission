#pragma once

/**
 * button enum represents all the buttons on the device
 */
enum BUTTON { PIN_KEY1 = 9, PIN_KEY2 = 8 };
/**
 * LED enum represents all the LEDS on the device
 */
enum LED { RED_LED = 4, GREEN_LED = 5 };

/**
 * keypress enum to indicate which key has been pressed
 */
enum keyPress { NO_KEY, KEY1, KEY2 };
